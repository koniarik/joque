name: test

on:
    push:
        branches:
            - main

jobs:
    test:
        runs-on: ubuntu-22.04	
        steps:
          - uses: actions/checkout@v2

          - name: configure
            run: make configure
          
          - name: build
            run: make build
          
          - name: test
            run: make test
